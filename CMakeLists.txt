cmake_minimum_required(VERSION 2.8)
project( motdetection )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( motdetection motdetection.cpp )
target_link_libraries( motdetection ${OpenCV_LIBS} )
